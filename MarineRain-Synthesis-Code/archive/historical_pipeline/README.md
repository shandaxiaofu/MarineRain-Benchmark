# Historical pipeline archive

These scripts are byte-identical copies of the historical source from
`/Users/gaohan/Desktop/syn_monadepth`, apart from the filename organization
performed before they were moved into this archive.

They are retained for provenance only. They contain hard-coded Windows paths,
historical parameters, overlapping implementations, and a haze model that does
not exactly match the manuscript equation.

Use the repository-root `generate_marinerain.py` entry point for the
paper-authoritative pipeline.
